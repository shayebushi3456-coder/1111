{
  "name": "work-eval",
  "version": "1.0.0",
  "private": true,
  "description": "WorkEval · Agent 自动化评测控制台",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.6"
  }
}
