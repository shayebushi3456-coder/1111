# 开发环境：Vite dev server 将 /api 代理到的 task-pilot 后端地址
# 对应后端 configs/config.example.yaml 的 server.addr（默认 :8081）
TASKPILOT_DEV_PROXY_TARGET=http://127.0.0.1:8081

# 生产构建：若前端与后端不同源部署，需设置为后端可访问的完整地址
# （task-pilot 未内置 CORS 中间件，跨域访问需后端另行配置反代/CORS 才能生效）。
# 留空时前端请求走相对路径 /api/v1/...，需要网关/反代把它转发到后端 8081 端口。
# VITE_API_BASE_URL=https://task-pilot.internal.example.com
